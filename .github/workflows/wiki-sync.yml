name: Wiki Sync Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain current wiki process
        run: |
          echo "GitHub Wiki is maintained directly in bon2362/book-club.wiki.git."
          echo "The old docs/features auto-sync is disabled because it can overwrite owner-facing Wiki pages with stale feature docs."
