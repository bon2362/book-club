# Лента времени: редизайн по дизайн-хендофу

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Привести `/timeline/[slug]` к виду и поведению из дизайн-хендофа:
полотно во всю ширину и высоту окна, тёплая поверхность, тихие метки,
раскладка без пропаж, легенда-фильтр.

**Architecture:** Правки точечные — почти весь целевой код в репозитории есть.
Меняются габариты страницы, оформление четырёх слоёв, логика раскладки в
`lib/timeline/geometry` и стартовый диапазон в `view-model.ts`. Добавляется
одна новая панель (легенда-фильтр). Геометрия остаётся чистой: измеритель
ширины текста передаётся параметром.

**Tech Stack:** Next.js App Router, React 18, CSS custom properties, Jest, Playwright.

**Источник:** `/Users/ekoshkin/Downloads/design_handoff_timeline/` —
`README.md` (спецификация), `DESIGN-RATIONALE.md` (обоснования),
`globals-additions.css` (токены), `prototype/` (референс).

## Что подтверждено по коду

| Утверждение хендофа | Проверка |
|---|---|
| `maxWidth: 1200px` зажимает полотно | подтверждено, `app/timeline/[slug]/page.tsx:40` |
| `EVENT_AREA_HEIGHT_PX = 380` | подтверждено, `components/nd/timeline/event-area.ts:10` |
| Ось рисуется `--border-strong` (#111) | подтверждено, `TimelineRuler.tsx:24` |
| Часть событий исчезает | **механизм другой**: строки `if (lane < 0) return` у нас нет. Раскладка возвращает дорожки сверх ёмкости, а слой обрезан `overflow: hidden` на 380 px — всё выше не видно. Следствие то же, лечится тем же |
| Загрузка картинок «как обложки книг» | **неверно**: у книг просто поле внешнего адреса, файлового хранилища в проекте нет |

## Шесть решений, принятых сверх хендофа

Хендоф принят целиком, кроме перечисленного. Каждое отклонение — с причиной.

1. **Существующие цвета типов мигрируются.** В базе лежат `#D97706`,
   `#2563EB`, `#009083` — именно они дают кричащие кружки. Новая палитра
   подействует только на типы, созданные после неё, поэтому три существующих
   обновляются разово на `--data-terracotta` / `--data-slate` / `--data-sage`.
   Без этого §2.4 хендофа не даст эффекта.

2. **Измеритель ширины текста передаётся параметром.** В прототипе ширина
   меряется `canvas.measureText`. Наша `lib/timeline/geometry` — чистые функции
   под Jest; в jsdom `measureText` вернёт ноль без нативного пакета, и раскладка
   схлопнется. Функция раскладки принимает `measure: (text, font) => number`:
   в браузере настоящий канвас, в тестах детерминированная оценка.

3. **Картинка — только по внешнему адресу.** Перетаскивание файла требует
   хранилища, которого в проекте нет. Рамка в карточке показывает картинку по
   `image_url`; поле адреса правится в админке, как у обложек книг. Настоящая
   загрузка — отдельная задача.

4. **Оболочка на всю высоту — только от 768 px.** Ниже страница остаётся
   обычной прокручиваемой: там вместо ленты вертикальный список на три десятка
   событий, и `overflow: hidden` его обрежет.

5. **Карточка растёт, полотно уступает.** Место под карточку зарезервировано
   (`min-height: 152px`), но описание бывает на два абзаца. Правило: карточка
   растёт до `40dvh`, дальше текст прокручивается внутри неё; полотно ужимается
   до своего минимума 200 px.

6. **Правка на полотне (§9–§11 хендофа) в этот заход не входит.** Инлайн-
   редактор, формы создания, призраки, поиск и новый флаг `hidden` в
   `timeline_events` — отдельный проект. Он частично отменяет админку, выкаченную
   ранее, и смешивать его с починкой вида значит получить один пул-реквест, в
   котором непонятно, что сломалось.

---

## File Structure

Создаётся:

```
components/nd/timeline/TimelineLegend.tsx      — легенда-фильтр над полотном
components/nd/timeline/measure-text.ts         — измеритель ширины через канвас
lib/timeline/layout.ts                         — раскладка с деградацией (+тест)
drizzle/0057_timeline_type_colors.sql          — миграция цветов типов
```

Изменяется:

```
app/globals.css                                — блок токенов из хендофа
app/timeline/[slug]/page.tsx                   — габариты, порядок блоков
components/nd/timeline/TimelineView.tsx        — сетка, карточка сверху, жесты
components/nd/timeline/TimelineEventLayer.tsx  — точка, интервал, кластер
components/nd/timeline/TimelineEpochLayer.tsx  — тинты, подпись, выбранная
components/nd/timeline/TimelineRuler.tsx       — тёплая ось, две засечки
components/nd/timeline/TimelineDetailCard.tsx  — метка «одна дата», картинка
components/nd/timeline/TimelineControls.tsx    — переезжает в легенду
components/nd/timeline/event-area.ts           — высота из измерения
components/nd/timeline/admin/palette.ts        — проекция токенов ДС
lib/timeline/view-model.ts                     — стартовый диапазон с проверкой
e2e/timeline-layout.spec.ts                    — новые проверки раскладки
docs/features/timeline.md, docs/wiki/Timelines.md
```

---

### Task 1: Токены и цвета данных

- [ ] **Шаг 1: Рабочая папка**

```bash
git fetch origin main
git worktree add ../book-club-timeline-redesign -b feat/timeline-redesign origin/main
cd ../book-club-timeline-redesign
ln -s ../book-club/node_modules node_modules
ln -sf /Users/ekoshkin/book-club/.env.local .env.local
ln -sf /Users/ekoshkin/book-club/.env.test.local .env.test.local
```

- [ ] **Шаг 2: Токены**

Вставить содержимое `globals-additions.css` в конец блока `:root` в
`app/globals.css`. Существующие имена не переопределяются — проверить это
глазами после вставки.

- [ ] **Шаг 3: Палитра админки — проекция токенов**

`components/nd/timeline/admin/palette.ts` заменить на пять цветов типов
(`#B0603C`, `#5D7290`, `#57795F`, `#7A5E86`, `#8A6B3A`) и шесть тинтов эпох
(`#EFE4D6`, `#EADFCB`, `#E1E3D2`, `#DCE2E4`, `#E9DCDC`, `#E3DCE6`) с именами.
Тинты предлагаются только для цвета полосы эпохи на ленте, цвета типов — только
для типов событий.

- [ ] **Шаг 4: Миграция существующих цветов**

`drizzle/0057_timeline_type_colors.sql` — три `UPDATE` по `title`:
«Книга» → `#B0603C`, «Событие» → `#5D7290`, «Личность» → `#57795F`.
Миграция идемпотентна: `WHERE color <> '<новый>'`.

Тест `drizzle/0057_timeline_type_colors.test.ts` — по образцу
`drizzle/0056_timeline_tables.test.ts`: проверяет, что в файле есть три UPDATE и
что новые значения совпадают с палитрой из `admin/palette.ts`.

- [ ] **Шаг 5: Проверки и коммит**

```bash
npm run lint && npm run typecheck && npm test
git add app/globals.css components/nd/timeline/admin/palette.ts drizzle/0057*
git commit -m "feat: токены полотна и палитра данных для ленты времени

E2E: не нужен на этом шаге — вида ещё не касаемся.
Wiki: нужна — правка идёт в задаче 6."
```

---

### Task 2: Габариты — главное лекарство

Этот шаг снимает две трети слипания подписей. Делать первым из визуальных.

- [ ] **Шаг 1: Страница**

`app/timeline/[slug]/page.tsx`: снять `maxWidth: 1200px`. Страница — колонка на
всю высоту окна **от 768 px**: `display:flex; flex-direction:column;
height:100dvh; overflow:hidden`. Ниже 768 px — обычная прокручиваемая страница
(там мобильный список, и обрезать его нельзя).

Поля — через `--tl-gut`, заданные каждому блоку отдельно, чтобы ось и полосы
эпох тянулись от края до края (`margin-inline: calc(-1 * var(--tl-gut))`).

Вертикальные поля: заголовок `1.5rem` сверху, `1rem` снизу.

- [ ] **Шаг 2: Порядок блоков**

`TimelineView.tsx`: карточка деталей **сверху**, распорка `flex:1`, полотно
приклеено к низу. Карточке — `min-height: 152px`, `max-height: 40dvh`, при
переполнении содержимое прокручивается внутри неё.

- [ ] **Шаг 3: Высота полотна из измерения**

`event-area.ts`: убрать константу `EVENT_AREA_HEIGHT_PX = 380`. Высота области
событий задаётся в CSS как `clamp(200px, 40dvh, 460px)`; фактическая читается
`ResizeObserver`-ом. Ёмкость дорожек — `floor((высота − 10 − 6) / 44)`.

- [ ] **Шаг 4: Осмотр**

Поднять сервер на изолированной базе, открыть `/timeline/vseobschaya-istoriya`,
снять экран. Ожидается: полотно во всю ширину, событий на дорожках заметно
больше, многоточий почти нет.

- [ ] **Шаг 5: Проверки и коммит**

---

### Task 3: Поверхность, метки, эпохи, линейка

Значения — из README хендофа, §2, §4, §5. Ниже только то, что легко пропустить.

- [ ] **Шаг 1: Поверхность**

Полотно — не карточка: **ни рамки, ни белого фона**. Фон страницы `--bg`,
сверху `1px solid var(--hair)`. Ось — `--tl-axis` во всю ширину окна.
Засечки — `--tl-tick` (8 px) и `--tl-tick-soft` (4 px). Волосок к оси —
`--tl-connector`, у выбранного `--accent` при `opacity .55`.

- [ ] **Шаг 2: Метки**

Точка 8 px сплошная, `box-shadow: 0 0 0 3px var(--bg)` — разрыв в волоске, а не
кольцо. **Иконка типа на полотне не рисуется вообще** — уходит в карточку и
подсказку. Подпись `--nd-sans` 0.845rem `--text`, дата `--nd-mono` 0.7rem
`--text-muted`. Выбранное: подпись 600, точка в кольце `--text`.

Интервал — линия 2 px цветом типа с засечками 1×8 px по концам; у выбранного
линия 3 px и заливка тем же цветом на `opacity .1`.

- [ ] **Шаг 3: Подсказка вместо `title`**

Карточка-подсказка: `position: fixed`, `translate(-50%,-100%)` над меткой, фон
`--bg-input`, `--radius-control`, `--shadow-pop`. **Гаснет при перетаскивании.**
Нативный `title` убрать — он залипает под курсором.

- [ ] **Шаг 4: Эпохи**

Заливка — бледный тинт сразу, без `opacity` поверх насыщенного хекса. Высота
21 px, шаг дорожки 26 px. Подпись `--nd-serif` 0.78rem `--text-body`. Подпись у
левого края: `margin-left: max(0, -left)`, `max-width` — видимая ширина. Полоса
уже 40 px — подписи нет. Выбранная: `inset 0 -2px 0 var(--text)`.

- [ ] **Шаг 5: Осмотр и коммит**

Снять экран, сверить с прототипом. Проверить `bash scripts/check-no-raw-hex.sh`
со списком изменённых файлов (без аргументов скрипт ничего не проверяет).

---

### Task 4: Раскладка без пропаж

Единственная задача плана, которую обязательно закрывать тестами.

- [ ] **Шаг 1: Измеритель отдельно**

`components/nd/timeline/measure-text.ts` — `createTextMeasurer()`: канвас,
кэш по строке+шрифту, шрифты те же, что в CSS. При отсутствии канваса —
детерминированная оценка по числу символов (текущая
`estimateEventLabelTextWidth`).

- [ ] **Шаг 2: Провальные тесты раскладки**

`lib/timeline/layout.test.ts`. Обязательные случаи:

1. подпись у правого края **отражается влево**, а не ужимается многоточием;
2. событие, не поместившееся с подписью, остаётся **точкой**, а не исчезает;
3. точки, не поместившиеся даже точкой, собираются в **кластер**; одиночка
   кластером не становится;
4. интервал **никогда не теряется**: не хватило дорожек — кладётся в наименее
   занятую;
5. полоса целиком за краем полотна → **подписи нет** (иначе «1914 — 1945»
   встаёт над участком 1860-х и врёт);
6. у правого края подпись интервала растёт **влево от начала полосы**.

Измеритель в тестах — подставной: ширина = длина строки × фиксированное число.
Так проверяется логика, а не шрифтовые метрики.

- [ ] **Шаг 3: Перенести раскладку**

`prototype/geometry.jsx` → `lib/timeline/layout.ts` с типами. Функции
`tlLayout`, `tlEpochLanes`, `tlTicks`, `tlFit`, `tlTransform`, `tlCoord`.
Существующие `lib/timeline/geometry/*` остаются там, где ещё используются;
дубли удалить.

- [ ] **Шаг 4: Проверки и коммит**

---

### Task 5: Стартовый вид, поведение, легенда-фильтр

- [ ] **Шаг 1: Стартовый диапазон с проверкой**

`lib/timeline/view-model.ts`: сохранённый диапазон берётся, если внутри него
не меньше `max(5, ceil(всего * 0.25))` элементов; иначе `fit-all`. Тест на обе
ветки: диапазон «Всеобщей истории» (4 из 31) отвергается, разумный принимается.

- [ ] **Шаг 2: Жесты**

Колесо без модификатора — панорама по горизонтали. `Ctrl`/`⌘` + колесо —
масштаб **под курсором**. Перетаскивание с порогом 3 px, чтобы клик после
сдвига не выделял. `+`, `−`, `F`, `Esc`. Ограничения зума: 2…40 000 лет.

- [ ] **Шаг 3: Выбор не дёргает полотно**

`selectEvent` больше не центрирует. Метка на экране — диапазон не меняется
вовсе. Ушла за край — минимальный сдвиг до отступа 80 px.

- [ ] **Шаг 4: Легенда-фильтр**

`TimelineLegend.tsx` — строка над полотном, `min-height: 42px`, всегда видна.
Чипы типов с точкой, названием и счётчиком; чип «Эпохи» с квадратной плашкой.
Выключенный: точка полая, текст зачёркнут. Кнопки масштаба справа.

Состояние берётся из данных ленты (`filter_type_ids`, `epochs_visible`) и
живёт локально на странице — сохранение в базу в этот заход не входит.

Чип «Вся база» **не делать**: он часть правки на полотне, вынесенной из захода.

- [ ] **Шаг 5: Проверки и коммит**

---

### Task 6: Тесты, документация, прогон

- [ ] **Шаг 1: E2E**

`e2e/timeline-layout.spec.ts`: убрать проверку «подписи не выходят за правый
край» (многоточия больше нет), добавить:
- подписи соседних событий не накладываются при 1280 и после двух «+»;
- на 375 px лента скрыта, виден вертикальный список, страница прокручивается;
- клик по событию не меняет диапазон, если метка на экране.

`e2e/timeline.spec.ts` — проверить, что не сломался после смены разметки.

- [ ] **Шаг 2: Прогон**

```bash
npm run test:e2e:focused -- e2e/timeline-layout.spec.ts
npm run test:e2e:focused -- e2e/timeline.spec.ts
npm run test:e2e:focused -- e2e/admin-timeline.spec.ts
```

- [ ] **Шаг 3: Документация**

`docs/features/timeline.md` — раздел «Визуальный язык»: токены полотна,
палитра данных, деградация раскладки, правило подписи интервала.
`docs/wiki/Timelines.md` — абзац для владельца: почему лента теперь во всю
ширину, что делает легенда, почему цвета приглушены.
`app/styleguide/page.tsx` — добавить палитру данных и тинты в витрину.

- [ ] **Шаг 4: PR**

- [ ] **Шаг 5: Миграция цветов по проду**

**После мержа, вручную** — Vercel миграции не применяет:

```bash
node --env-file=.env.local scripts/apply-migration.mjs drizzle/0057_timeline_type_colors.sql
```

---

## Self-Review

**Покрытие хендофа.** §1 габариты — задача 2. §2 поверхность, §4 метки,
§5 эпохи — задача 3. §3 стартовый вид, §7 поведение, §8 легенда — задача 5.
§6 раскладка — задача 4. §9–§11 (правка на полотне) сознательно вынесены,
основание — в разделе «Шесть решений».

**Что не делается.** Правка на полотне, чип «Вся база», загрузка файлов,
сохранение состояния фильтра в базу, флаг `hidden` в `timeline_events`.

**Риск.** Задача 2 меняет габариты страницы целиком; на узком окне карточка и
полотно конкурируют за высоту. Правило разрешения задано (карточка до 40dvh,
полотно до минимума 200 px) и проверяется осмотром.
