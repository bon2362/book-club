/**
 * Участник matching-сессии считается «онлайн», если опрашивал версию за последние
 * 12 секунд. Опрос `/api/matching/version` одновременно служит heartbeat'ом.
 *
 * Вынесено в отдельный leaf-модуль без db-зависимостей: значение нужно и серверной
 * `presence.ts`, и клиентскому `poll-interval.ts` (адаптивный интервал не должен
 * превышать это окно). Импортировать `presence.ts` в client-бандл нельзя — он тянет `db`.
 */
export const PRESENCE_WINDOW_MS = 12_000
