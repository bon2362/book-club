import { notFound,redirect } from 'next/navigation'; import { auth } from '@/lib/auth'; import { loadCollectionById,loadEditorBooks,serializeCollection } from '@/lib/collections/repo'; import { isCollectionOwner } from '@/lib/collections/rules'; import { viewerFromSession } from '@/lib/collections/http'; import CollectionEditorPage from '@/components/nd/CollectionEditorPage'
export const dynamic='force-dynamic'; export default async function Page({params}:{params:{slugOrId:string}}){const s=await auth();if(!s?.user?.id)redirect(`/collections/${params.slugOrId}`);const c=await loadCollectionById(params.slugOrId).catch(()=>null);if(!c)notFound();const v=viewerFromSession(s);if(!isCollectionOwner(c,v)){if(v.isAdmin)redirect(`/admin?view=collections&collection=${c.id}`);notFound()}return <CollectionEditorPage isAdmin={v.isAdmin} initial={serializeCollection(c)} initialBooks={await loadEditorBooks(c.bookIds)}/>}
