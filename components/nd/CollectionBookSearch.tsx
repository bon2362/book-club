'use client'
import { useEffect, useState } from 'react'
import type { CollectionBookSearchResult } from '@/lib/collections/types'
export default function CollectionBookSearch({excludeIds,onAdd}:{excludeIds:ReadonlySet<string>;onAdd:(book:CollectionBookSearchResult)=>void}) { const [q,setQ]=useState('');const [items,setItems]=useState<CollectionBookSearchResult[]>([]); useEffect(()=>{if(q.trim().length<2){setItems([]);return};const t=window.setTimeout(async()=>{const r=await fetch(`/api/collections/book-search?q=${encodeURIComponent(q.trim())}`);const d=await r.json();setItems(r.ok?d.books:[])},250);return()=>clearTimeout(t)},[q]);const shown=items.filter(x=>!excludeIds.has(x.id));return <div><input className="p-input" aria-label="Найти книгу" placeholder="Найти книгу по названию или автору" value={q} onChange={e=>setQ(e.target.value)}/>{q.trim().length>=2&&<div role="listbox">{shown.map(book=><button key={book.id} type="button" role="option" aria-label={book.title} onClick={()=>{onAdd(book);setQ('')}}>{book.title} · {book.author}</button>)}{shown.length===0&&<p>В каталоге такой книги нет. В подборку можно добавить только книги из каталога — сначала предложите её в каталоге.</p>}</div>}</div> }
