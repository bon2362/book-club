import type { CollectionStatus } from '@/lib/collections/types'
import { COLLECTION_REASON_PREFIX, COLLECTION_STATUS_LABEL } from './collection-status'
export default function CollectionStatusBanner({ status, reason }: { status: CollectionStatus; reason: string | null }) { const prefix = COLLECTION_REASON_PREFIX[status]; return <div data-testid="collection-status-banner" style={{ display: 'flex', gap: 12, padding: '13px 16px', border: '1px solid var(--border)', borderLeft: '2px solid var(--accent)', background: 'var(--bg-tint)' }}><span style={{ fontSize: '0.6rem', textTransform: 'uppercase', letterSpacing: '0.12em', color: 'var(--accent)' }}>{COLLECTION_STATUS_LABEL[status]}</span><div style={{ fontSize: 12, color: 'var(--text-body)' }}>Подборка не опубликована. Её видите вы как автор и модератор.{prefix && reason && <div><b>{prefix}</b> {reason}</div>}</div></div> }
