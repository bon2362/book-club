'use client'
import { useMatchingBoard } from './MatchingBoardProvider'
export default function MatchingWorkspace({children}:{children:React.ReactNode}){const{pending}=useMatchingBoard();return <section data-testid="matching-scenarios-workspace" aria-busy={pending} style={{position:'relative',minHeight:0,height:'100%',display:'flex',flexDirection:'column',overflow:'hidden',background:'var(--bg)',borderTop:'1px solid var(--hair)'}}>{pending&&<div data-testid="matching-board-loader" style={{position:'absolute',inset:0,zIndex:2,display:'grid',placeItems:'center',pointerEvents:'none'}}><span className="nd-board-spinner"/></div>}<div data-testid="matching-scenarios-scroll" style={{flex:1,minHeight:0,overflowY:'auto',padding:'0.75rem 0.7rem 1rem',opacity:pending?0.45:1,transition:'opacity 0.25s ease'}}>{children}</div><div aria-hidden style={{position:'absolute',left:0,right:0,bottom:0,height:52,pointerEvents:'none',background:'linear-gradient(transparent, var(--bg))'}}/></section>}
